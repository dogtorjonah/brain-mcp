/**
 * Identity store barrel — re-exports the IdentityStore class and types.
 */
export { IdentityStore } from './store.js';
export type {
  IdentityProfile,
  ChainEvent,
  IdentitySop,
  HandoffNote,
  SpecialtySignature,
} from './store.js';
